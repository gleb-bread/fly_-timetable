const token = import.meta.env.VITE_API_TOKEN;
const project = import.meta.env.VITE_API_PROJECT;
const page = import.meta.env.VITE_API_PAGE;

export const Cookie = {
    token,
    project,
    page,
};
